global:
  scrape_interval: "1s"

scrape_configs:
  - job_name: "teiserver"
    static_configs:
      # for docker, may need "host.docker.internal:4001"
      - targets: ["host.containers.internal:4001"]
